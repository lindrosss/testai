# Необязательно, если порт HTTP задаётся через .env (см. .env.example в этом каталоге).
# Пример переопределения только nginx — скопируйте в docker-compose.override.yml при необходимости.

services:
  nginx:
    ports:
      - "8080:80"
